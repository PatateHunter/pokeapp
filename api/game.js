export const startGame = () =>{
    const pokemon = pokemon[Math.floor(Math.random()* 5)];
    const turn = turn[Math.floor.random(Math.random()* 2)];
    /// random math lab
    // socket.emit
};

export let name = 'ichai';

